%nav.navbar-default.stuckMenu.isStuck{:role => "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{"data-target" => ".collapse.navbar-collapse", "data-toggle" => "collapse", :type => "button"}
      = link_to 'Otoshimonno', root_path, class: 'navbar-brand'
    .collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li
            = link_to 'new card', new_user_item_path(current_user)
          %li.dropdown
            %a.dropdown-toggle{"aria-expanded" => "false", "data-toggle" => "dropdown", :href => "#", :role => "button"}
              = image_tag current_user.avatar, class: 'img img-circle', size: '30x30'
              = current_user.nickname
              %span.caret
            %ul.dropdown-menu
              %li= link_to 'マイページ', user_path(current_user)
              %li= link_to 'アイテム一覧', user_items_path(current_user)
              %li= link_to 'チュートリアル', help_path
              %li.divider
              %li= link_to 'プロフィールの変更', edit_user_path(current_user)
              %li= link_to 'サインアウト', destroy_user_session_path, method: :delete
        - else
          %li= link_to 'サインイン', new_user_session_path
          %li
            %a{:href => "/help"} ヘルプ
